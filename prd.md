# RushMeme 产品需求文档

> 版本：1.0 │ 更新日期：2025-10-16 │ 负责人：产品团队 / RushMeme  
> 文档目标：明确 RushMeme 桌面应用的产品范围、交互流程与验收标准，指导后续设计与研发。

## 1. 产品概述

RushMeme 是一款面向加密资产交易与投研用户的桌面应用。产品通过全局快捷键获取当前剪贴板中的合约地址，一次性在多个行情或工具平台中打开对应页面，降低用户在各平台之间反复切换、粘贴的时间成本。

### 1.1 产品定位

-   **平台形态**：Electron 桌面应用（macOS / Windows / Linux）。
-   **核心价值**：把合约地址检索、筛选、跳转的行为整合为一条顺滑的链路。
-   **核心人群**：需要快速查看多平台行情信息的币圈投研、操盘与社区运营人员。

### 1.2 产品目标

1. 快速：从复制地址到打开目标平台，操作 ≤3 步。
2. 可配置：满足用户对目标平台、快捷键、通知方式的个性化需求。
3. 稳定：保证在多操作系统环境下的稳定运行与状态反馈。

## 2. 用户画像与场景

| 角色     | 典型目标                           | 痛点                             |
| -------- | ---------------------------------- | -------------------------------- |
| 交易员   | 快速同步多平台盘口、新闻、链上数据 | 手动在多个站点粘贴地址耗时、易漏 |
| 社群运营 | 实时洞察热门代币动态并分享         | 需要快捷收集素材、截图、链接     |
| 研究员   | 对项目做合规或深度分析             | 需要校验多个数据源，高频重复操作 |

### 2.1 核心使用旅程

1. 启动 RushMeme → 查看配置界面。
2. 通过 “Add platform” 添加需要监控的工具平台/分析站点。
3. 为每个平台设置快捷键、启用状态等。
4. 复制任意合约地址 → 触发全局快捷键 → 自动打开已启用平台页面。
5. 根据状态提示与通知确认执行结果。

## 3. 功能需求

### 3.1 平台管理

-   列表展示用户已配置的平台卡片
-   卡片包含：平台名称、Token 类型选择、快捷键输入、设置入口、启用开关。
-   Token 类型以文本形式展示预设链（如 BSC、Solana、Base、X Layer、EVM、Any）,通常 Solana 可以和其他 EVM 链放一起作为组合选项，例如 BSC|Solana，Base|Solana，XLayer|Solana。
-   支持平台启用/禁用；禁用状态不会在快捷键触发时打开。
-   提供 Pro 版本，通过序列号升级为 pro，每个序列号支持 2 个设备

### 3.2 平台预设与新增

-   “Add platform” 按钮与标题同行，默认右对齐。
-   点击打开下拉菜单，展示内置平台模板（Binance Wallet、DexScreener、GMGN 等）。
-   下拉菜单提供 “Custom platform…” 选项，允许自定义名称、链模板、URL 模板。
-   下拉组件需具备最高层级，避免被弹窗或其他内容遮挡。

### 3.3 平台配置弹窗

-   触发方式：点击卡片右上角的设置按钮或新增自定义平台。
-   弹窗字段：名称、Token 类型、快捷键、URL 模板（按链划分）、平台启用开关。
-   校验规则：URL 模板必须包含 `{CA}` 占位符；快捷键不可冲突（与现有平台相同快捷键则视为批量打开）；Token 类型需匹配预设集合。
-   url 中的代币类型写为 [bsc|sol}] -https://web3.binance.com/en/token/bsc/{CA}
    https://pump.fun/coin/{CA}
    https://web3.okx.com/token/bsc/{CA}
    https://web3.okx.com/token/solana/{CA}

### 3.4 快捷键与地址捕获

-   默认全局快捷键 `Cmd + Shift + C`，可在设置中调整，按平台修改 Cmd。
-   默认在前置窗口执行复制指令后再获取剪贴板
-   提取剪贴板中符合正则规则的合约地址（EVM 地址、Solana 公钥等）。
-   若剪贴板无有效地址，给出错误提示（弹窗或通知）。
-   可自定义执行后的延迟时间，默认 300ms，可在 0–1000ms 范围调整。（作为 pro 功能，默认为 1000ms）

### 3.5 通知与状态反馈

-   自动保存：配置变更自动保存并显示 “Saving… / Saved / Save failed” 状态。
-   操作结果：打开平台成功、失败时，可选择是否推送系统通知。
-   页面状态条（StatusBanner）：在配置界面底部显示最近一次操作反馈，可关闭。

### 3.6 数据持久化与恢复

-   所有配置存储在本地（可使用 JSON 文件或 Electron store）。
-   支持从预设恢复：一键恢复到默认平台列表与配置。
-   允许导出/导入配置（可选迭代功能）。

## 4. 非功能需求

### 4.1 性能与稳定性

-   启动时间 ≤ 2 秒（冷启动，未加载平台页面）。
-   快捷键触发到打开所有启用平台窗口 ≤ 1.5 秒（取决于浏览器启动）。
-   支持同时打开 ≥ 6 个平台链接，无崩溃或卡顿。

### 4.2 兼容性

-   操作系统：macOS 13+、Windows 10+、主流 Linux 发行版。
-   浏览器：使用默认系统浏览器打开链接，不依赖特定浏览器。
-   键盘布局：需兼容 Mac（Command）与 Windows（Ctrl）、Linux 不同按键命名。

## 5. 数据结构概要

-   `PlatformConfig`：`{ id, key?, name, tokenType, urls: { evm?, sol?, any? }, shortcut, enabled }`
-   `NotificationConfig`：`{ showOnSuccess: boolean, showOnError: boolean }`
-   `CopySimulationConfig`：`{ enabled: boolean, delayMs: number }`
-   `AppConfig`：整合平台数组、通知、复制、版本等信息。

## 6. 交互与界面规范

完全使用 shadui 构建前端的组件，使用 shadui 案例中的样式

## 7. 关键指标（后续埋点）

-   平均每日启动次数 / 活跃用户数。
-   单日快捷键触发次数 & 成功率。
-   常用平台分布（用于优化默认模板）。
-   配置改动留存率（用户是否继续使用自定义配置）。
    通过 GA 可以实现这些指标获取吗

## 8. 风险与边界

-   剪贴板权限：首次使用时需要用户授权；若授权失败需引导用户到系统设置开启。

---
